import React from 'react';

export default React.createClass({

  render() {
    return (
      <div className="navbar">
        <button onNavigate={}>Home</button>
        <button>Logout</button>
      </div>
    );
  }
}); 
